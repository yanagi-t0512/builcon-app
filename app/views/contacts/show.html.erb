<div class="d-flex align-items-center mt-4 mb-4">
  <h2>問合せ詳細</h2>
  <div class="ml-auto contacts__linkBox">
    <%= link_to '一覧', contacts_path, class: 'btn btn-outline-dark' %>
    <%= link_to '編集', edit_contact_path(@contact), class: 'btn btn-outline-dark' %>
  </div>
</div>

<%# <%= @building.property_name %>
<% if flash[:notice] %>
  <div class="alert alert-primary"><%= flash[:notice] %></div>
<% end %>

<%= render @contact %> 
